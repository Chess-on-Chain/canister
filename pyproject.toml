[project]
name = "chessonchain"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = "==3.10.*"
dependencies = [
    "kybra==0.7.1",
    "setuptools==79.*",
]

[dependency-groups]
dev = [
    "ic-py>=1.0.1",
    "isort>=6.0.1",
]
